# Crawl4AI Scripts Scaffold

This directory is reserved for future Crawl4AI-backed extraction scripts.

Planned files:
- `job_search.py` — crawl result pages and emit normalized job cards
- `jd_extract.py` — crawl a detail page and emit structured JD fields
- `interview_extract.py` — crawl discussion/interview pages and emit structured interview-prep records

Keep extraction logic here. Keep business ranking and downstream decisions in skills.
